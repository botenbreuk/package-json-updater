import QtQuick
import QtQuick.Controls.Basic
import App

Menu {
    id: menu
    padding: 4

    background: Rectangle {
        implicitWidth: 200
        radius: 8
        color: Theme.surface
        border.width: 1
        border.color: Theme.border
    }
}
